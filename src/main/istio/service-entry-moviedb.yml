apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: redis-rule
spec:
  hosts:
  - api.themoviedb.org
  location: MESH_EXTERNAL
  ports:
  - number: 443
    name: https
    protocol: TLS